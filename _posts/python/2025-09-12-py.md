---
layout: post
title:  python try expect
date:   2025-09-12 09:01:00 +0800
image: 04.jpg
tags: 
    - python
---

```py
try:
    file = open("my_file.txt", "r")
    content = file.read()
except FileNotFoundError:
    print("错误：文件不存在。")
else : 
    print("文件内容读取成功。")
finally:
    # 这行代码总会执行，确保文件被关闭，防止资源泄漏
    # 这里需要一个更严谨的结构来处理 file 变量可能不存在的情况
    # 但这个例子主要说明 finally 的用途
    print("程序执行完毕。")
    # if 'file' in locals() and not file.closed:
    #     file.close()
```