---
layout: post
title:  python flask-wtf-simple-2
date:   2025-11-12 11:24:29 +0800
tags: 
    - python 
    - flask
image: 07.jpg
---

form 的编写逻辑

```html
<!DOCTYPE html>
<html>
<head>
    <title>用户注册</title>
    <style>
        .error { color: red; font-size: 0.9em; }
        .success { color: green; font-weight: bold; }
        .field { margin-bottom: 15px; }
    </style>
</head>
<body>
    <h1>用户注册 (Sign Up)</h1>

    {% with messages = get_flashed_messages(with_categories=True) %}
        {% if messages %}
            <ul class="flashes">
            {% for category, message in messages %}
                <li class="{{ category }}">{{ message }}</li>
            {% endfor %}
            </ul>
        {% endif %}
    {% endwith %}

    <form method="POST" action="{{ url_for('signup') }}"> 
        {{ form.hidden_tag() }} 

        <div class="field">
            {{ form.email.label }}<br>
            {{ form.email(size=30) }}
            {% for error in form.email.errors %}
                <span class="error">[{{ error }}]</span>
            {% endfor %}
        </div>

        <div class="field">
            {{ form.username.label }}<br>
            {{ form.username(size=30) }}
            {% for error in form.username.errors %}
                <span class="error">[{{ error }}]</span>
            {% endfor %}
        </div>

        <div class="field">
            {{ form.age.label }}<br>
            {{ form.age(size=30) }}
            {% for error in form.age.errors %}
                <span class="error">[{{ error }}]</span>
            {% endfor %}
        </div>
        
        <br>
        
        {{ form.submit() }}
    </form>
</body>
</html>
```